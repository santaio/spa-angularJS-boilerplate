(function() {
  
  'use strict';

  angular
    .module('app')
    .constant('CONST', {
      API: {
        homolog: function() {
          var host = window.location.hostname;
          var path;
          
          return path;
        }
      }
    });
})();
